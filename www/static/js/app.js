(function () {    var lcReqApp = angular.module("lcReqApp", []);    lcReqApp        .filter("methodName", [function () {            return function (method) {                switch (method) {                    case 1:                        return "GET";                    case 2:                        return "POST";                    case 3:                        return "PUT";                    case 4:                        return "DELETE";                    default :                        return "Unknown";                }            }        }])        .directive('clickSelect', [            function () {                return {                    link: function (scope, element, attr, ctrl) {                        element.bind('click', function () {                            element.select();                        });                    }                };            }        ])        .controller("inspectListCtrl", ["$scope", "$http", "$location", function ($scope, $http, $location) {            var vm = $scope.vm = {};            vm.url = location.origin + "/" + $scope.reqId;            $http.get("/api/requests/" + $scope.reqId + "/inspects").success(function (result) {                $scope.inspects = result.data;            });        }])        .controller("homeCtrl", ["$scope", "$http", "$location", function ($scope, $http, $location) {            $http.get("/api/requests/").success(function (result) {                $scope.requests = result.data;            });        }]);})();